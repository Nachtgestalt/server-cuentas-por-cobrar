spring:
  datasource:
    url: jdbc:mysql://localhost:3308/cuentas_por_cobrar?useSSL=false
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
          hibernate:
            dialect: org.hibernate.dialect.MySQL5Dialect